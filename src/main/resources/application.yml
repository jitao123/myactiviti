server:
  port: 9099
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/myactiviti?characterEncoding=utf8&useSSL=true&serverTimezone=GMT%2B8&rewriteBatchedStatements=true&nullCatalogMeansCurrent=true
    username: root
    password: password
  activiti:
    history-level: full # 历史记录等级 全部
    db-history-used: true # 使用历史记录
    check-process-definitions: false # 检查流程定义